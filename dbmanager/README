insert, find parallel corpora

Need mongodb, setup db info in run.sh
python init.py initiates db
run mongodb.sh once before start

To start,
pip install -r requirements.txt
./run.sh

TODO: make better page, add package management
